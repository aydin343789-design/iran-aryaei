# منبع داده شهرها

در Build روی GitHub، فایل `scripts/build-city-data.py` داده شهرهای ایران را از پروژه `arnpacc/iran-city-coordinates` دریافت و داخل `www/cities.json` بسته‌بندی می‌کند.

منبع تحت MIT منتشر شده است و داده‌ها در زمان Build وارد APK می‌شوند؛ بنابراین برنامه پس از نصب برای فاصله‌یابی به اینترنت نیاز ندارد.
