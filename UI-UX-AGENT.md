# طراحی UI/UX نسخه 3 — سبک AI Agent

این نسخه رابط کاربری را از یک «لیست ابزار» به یک «workspace هوشمند آفلاین» تبدیل می‌کند.

## اصول طراحی
- Mobile-first و مناسب استفاده با یک دست
- Hero شبیه داشبورد دستیار هوشمند
- ورودی زبان طبیعی برای پیدا کردن ابزار، بدون API و بدون اینترنت
- Quick Actions برای سه کار پرتکرار
- کارت‌های ابزار با وضعیت «آماده»
- Bottom Navigation ساده: خانه، فعالیت، تنظیمات
- Sheet برای اجرای ابزارها، مناسب لمس موبایل
- نمایش خروجی اخیر به شکل نوار شناور
- حالت تاریک و نمایش فشرده
- همه متن‌ها و پردازش‌ها محلی هستند

## رفتار دستیار
ورودی‌هایی مانند:
- «حجم عکس را کم کن»
- «QR بساز»
- «عکس را PDF کن»
- «پس زمینه عکس را حذف کن»

با موتور keyword داخلی به ابزار مناسب هدایت می‌شوند. این بخش عمداً AI آنلاین نیست تا اصل «کاملاً آفلاین» حفظ شود.

## نکته GitHub
هیچ فایل UI به CDN یا فونت خارجی وابسته نیست؛ بنابراین GitHub Actions می‌تواند پروژه را بدون سرویس خارجی runtime بسازد و APK حاصل در زمان اجرا نیازی به اینترنت ندارد.
