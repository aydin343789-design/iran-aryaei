{
  "name": "iran-aryaei",
  "version": "1.0.0",
  "description": "ایران آریایی — جعبه‌ابزار آفلاین برای کاربران فارسی‌زبان",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
